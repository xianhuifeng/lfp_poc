[tool.poetry]
name = "lfd-poc"
version = "0.1.0"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
fastapi = "^0.115.0"
uvicorn = {extras = ["standard"], version = "^0.30.0"}
pydantic = "^2.7.0"
openai = "^1.40.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.0"
